The lowercase, wide-tracked 11px label that titles every region ("task lists", "calendar", "done").

```jsx
<Eyebrow>the rest — open when you're ready</Eyebrow>
```

Section labels are never bold headings — always this eyebrow. Set them all-lowercase and let the .14em tracking do the labelling, and keep them conversational ("open when you're ready").
